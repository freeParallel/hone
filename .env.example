VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Sentry DSN for client (public)
VITE_SENTRY_DSN=
# Sentry DSN for server (private)
SENTRY_DSN=
# PostHog (client analytics)
VITE_POSTHOG_KEY=
# Optional: override default US host. For EU, use https://eu.i.posthog.com
VITE_POSTHOG_HOST=
# Enable PostHog in local dev (0/1). When 0, PostHog is only initialized in production.
VITE_POSTHOG_ENABLE_DEV=0
# Server-side only (never expose on client)
SUPABASE_SERVICE_ROLE_KEY=

